import { StyleSheet } from 'react-native';

export const cardStyles = StyleSheet.create({
  container: {
    padding: 8,
    backgroundColor: '#e6e6e6',
    borderRadius: 4,
    gap: 8,
  },
  containerOut: {
    padding: 8,
    gap: 8
  }
});
